services:
  - type: web
    name: ontario-it-programs-api
    env: python
    buildCommand: pip install -r ai-system-web/backend/requirements.txt
    startCommand: uvicorn ai-system-web.backend.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
